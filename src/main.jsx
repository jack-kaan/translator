import React from 'react';
import ReactDOM from 'react-dom/client';
import PaperFlow from '../sample.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PaperFlow />
  </React.StrictMode>,
);
